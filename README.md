# Pebble-BakugoWatchface
A watchface made using C, instead of Alloy.
Better memory management, explosive text, bakugo, battery gauge inside of the equipment.

The rotational text is from a library that never ended up being created as a dependency from another user called special-draw, so it was pulled in here and tweaked to work for our use-case.
